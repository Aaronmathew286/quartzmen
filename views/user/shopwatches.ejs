<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Watch page</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link href="https://fonts.googleapis.com/css2?family=Almendra+SC&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
        integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <!-- Custom CSS -->
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            display: flex;
            flex-direction: column;
        }

        body {
            background-color: #001131;
            color: white;
        }

        #content {
            flex: 1;
            min-height: calc(100vh - 200px);
        }

        .navbar {
            background-color: #1d1e1e;
            position: relative;
            z-index: 1020;
        }

        .navbar-brand {
            font-family: 'Almendra SC', serif;
            color: #FFDD00;
        }

        .navbar,
        .navbar .nav-link,
        .navbar .navbar-text {
            color: white;
        }

        .navbar .nav-link:hover,
        .navbar .nav-link:focus,
        .navbar .dropdown-item {
            color: #ffdd00;
        }

        .navbar .dropdown-menu {
            background-color: #1d1e1e;
            z-index: 1050;
        }

        .navbar .dropdown-item {
            color: white;
        }

        .navbar .dropdown-item:hover {
            background-color: #ffdd00;
            color: #000000;
        }

        .navbar .navbar-toggler .navbar-toggler-icon {
            color: #ffffff;
        }

        .watch-category {
            text-align: center;
            padding-top: 10px;
        }

        .form-inline .form-control {
            border: none;
            margin-left: 50px;
        }

        .input-group-append {
            padding-left: 20px;
        }

        .rounded-pill {
            border-radius: 50px !important;
        }

        .rounded-pill .input-group-append .btn {
            border-top-right-radius: 50px !important;
            border-bottom-right-radius: 50px !important;
        }

        .second-nav {
            background-color: rgba(0, 0, 0, 0.822);
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
        }

        .second-nav .nav-link {
            text-transform: capitalize;
        }

        .carousel-inner img {
            width: 100%;
            height: 700px;
            object-fit: cover;
        }

        .nav-item .fa-solid:hover {
            color: #FFDD00;
        }

        .custom-navbar.navbar-nav {
            margin-top: 0;
        }

        .custom-navbar.nav-link {
            color: #333;
            transition: color 0.3s ease-in-out;
        }

        .custom-navbar.nav-link:hover {
            color: #007bff;
        }

        .material-symbols-outlined {
            margin-bottom: 2px;
            color: #FFDD00;
        }

        .carousel-control-prev,
        .carousel-control-next {
            width: 5%;
        }

        .carousel-control-prev:hover,
        .carousel-control-next:hover {
            background: none;
        }

        .footer {
            background-color: #000;
            color: #fff;
            padding: 20px 0;
        }

        .footer a:hover {
            color: yellow;
        }

        #priceDisplay {
            margin-top: 10px;
        }

        .scrolling-container {
            display: flex;
            overflow-x: auto;
            background-color: white;
            /* Set the background color to white */
            padding: 10px;
            /* Optional: Add padding if you want space around the content */

        }

        .logo {
            flex: 0 0 auto;
            /* Prevent logos from shrinking */
            margin-right: 20px;
            /* Optional: Add spacing between logos */
        }

        .logo img {
            height: 60px;
            /* Set the desired height for all images */
            width: auto;
            /* Maintain aspect ratio */
            display: block;
            /* Ensure images display correctly */
        }

        /* Optional: Add styles to hide scrollbars */
        .scrolling-container::-webkit-scrollbar {
            display: none;
            /* Hide scrollbar for WebKit browsers */
        }



        .card {
            background-color: #F4FCFF;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            transition: 0.3s;
            width: 100%;
            border-radius: 5px;
            color: #000;
            height: auto;
            /* Changed from fixed height to auto */
            overflow: hidden;
            margin-bottom: 20px;
            /* Added bottom margin */
        }

        .card-img-top {
            width: 100%;
            height: 250px;
            object-fit: cover;
        }

        .card-body {
            padding: 20px;
        }

        .card-title-model,
        .card-title {
            font-size: 24px;
            margin-bottom: 10px;
        }

        .card-text {
            font-size: 16px;
            line-height: 1.5;
            margin-bottom: 10px;
        }

        .card:hover {
            transform: scale(1.05);
            /* Increased scale slightly */
            transition: transform 0.3s ease-in-out;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.25);
            /* Increased shadow on hover */
        }

        .card-img-top i {
            position: absolute;
            top: 5px;
            right: 5px;
            font-size: 20px;
            color: rgb(0, 0, 0);
        }


        .fa-heart {
            color: rgb(255, 255, 255);
            /* Default color */
        }

        .fa-heart.liked {
            color: red;
            /* Color when liked */
        }

        .container {
            margin-bottom: 50px;
        }
    </style>

</head>

<body>
    <nav class="navbar navbar-expand-lg">
        <a href="/" class="logo" style="text-decoration: none;">
            <div class="navbar-brand-container d-flex align-items-center">
                <div class="navbar-brand">Quartz
                    <span class="material-symbols-outlined ms-2">watch_wake</span>
                    Men
                </div>
            </div>
        </a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
            aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <form class="form-inline mx-auto" action="/search" method="GET">
                <div class="input-group">
                    <input type="text" name="q" class="form-control border-0 rounded-pill bg-white text-black"
                        placeholder="Search" aria-label="Search">
                    <div class="input-group-append">
                        <button class="btn btn-outline-secondary rounded-pill" type="submit">
                            <i class="fas fa-search text-white"></i>
                        </button>
                    </div>
                </div>
            </form>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/wishlist" role="button" aria-haspopup="true" aria-expanded="false">
                        <i class="fa-solid fa-heart"></i>
                    </a>
                </li>
                <% if (loggedIn) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link" href="/profile" role="button" aria-haspopup="true"
                        aria-expanded="false">
                            <i class="fa-solid fa-user"></i>
                        </a>
                    </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/signup"><i class="fa-solid fa-user"></i>Sign Up</a>
                        </li>
                        <% } %>
                            <li class="nav-item">
                                <a class="nav-link" href="/cart" role="button" aria-haspopup="true"
                                    aria-expanded="false">
                                    <i class="fa-solid fa-cart-shopping"></i>
                                </a>
                            </li>
            </ul>
        </div>
    </nav>


    <div id="content">
        <h2 class="watch-category">Quartz Men Shop</h2>

        <!-- Sort & Filter -->
        <div class="container mt-5">
            <button id="filterButton" class="btn btn-light" data-bs-toggle="offcanvas"
                data-bs-target="#offcanvasFilters" aria-controls="offcanvasFilters">Sort & Filters</button>
                <button class="btn btn-light" id="cancelFilters" onclick="clearFilters()">Undo</button>
            <div class="offcanvas offcanvas-start" tabindex="-1" id="offcanvasFilters"
                aria-labelledby="offcanvasFiltersLabel">
                <div class="offcanvas-header">
                    <h5 id="offcanvasFiltersLabel">Filters</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                </div>
                <div class="offcanvas-body">
                    <form id="filter-form" action="/shopwatches" method="GET">

                        <div class="form-group mb-3">
                            <label for="sortBy">Sort by:</label>
                            <select id="sortBy" class="form-control" name="sortBy">
                                <option value="-">-</option>
                                <option value="priceAsc">Low to High</option>
                                <option value="priceDesc">High to Low</option>
                                <option value="brandAsc">Brand: A-Z</option>
                                <option value="brandDesc">Brand: Z-A</option>
                            </select>
                        </div>
                        <div class="form-group mb-3">
                            <label for="category">Category:</label>
                            <select id="category" class="form-control" name="category">
                                <option value="-">-</option>
                                <% categories.forEach(function(category) { %>
                                    <option value="<%= category.categoryName %>">
                                        <%= category.categoryName %>
                                    </option>
                                    <% }); %>
                            </select>
                        </div>
                        <div class="form-group mb-3">
                            <label for="gender">Gender:</label>
                            <select id="gender" class="form-control" name="gender">
                                <option value="-">-</option>
                                <option value="Male">Male</option>
                                <option value="Female">Female</option>
                            </select>
                        </div>
                        <div class="form-group mb-3">
                            <label for="strapMaterial">Strap Material:</label>
                            <select id="strapMaterial" class="form-control" name="strapMaterial">
                                <option value="-">-</option>
                                <option value="leather strap">Leather Strap</option>
                                <option value="stainless steel">Stainless Steel</option>
                                <option value="nylon strap">Nylon Strap</option>
                                <option value="silicon rubber">Silicon Rubber</option>
                                <option value="ceramic band">Ceramic Band</option>
                                <option value="mesh band">Mesh Band</option>
                                <option value="titanium band">Titanium Band</option>
                            </select>
                        </div>

                        <div class="form-group mb-3">
                            <label for="priceRange">Price:</label>
                            <input type="range" id="priceRange" class="form-control-range" name="priceRange" min="0" max="100000" step="100" value="0">
                            <div id="priceDisplay">Selected Price: ₹0</div>
                        </div>
                        <button type="submit" class="btn btn-primary">Apply Filters</button>
                        <button type="reset" class="btn btn-secondary">Cancel</button>
                    </form>
                </div>
            </div>
        </div>



        <!-- Product Container -->
        <div class="container product-container">
            <div class="row">
                <% if (noProductsMessage) { %>
                    <h2 style="justify-content: center; text-align: center;"><%= noProductsMessage %></h2>
                <% } else { %>
                <% products.forEach((product)=> { %>
                    <a href="/productdetail/<%= product._id %>" class="col-md-3"
                        style="text-decoration: none; color: inherit;">
                        <div class="card">
                            <div class="card-img-top position-relative">
                        <!-- Wishlist Heart Icon -->
                        <i class="<%= product.wishlist ? 'fas fa-heart liked' : 'far fa-heart' %>"
                            data-product-id="<%= product._id %>"
                            style="cursor: pointer;"
                            onclick="toggleWishlist(this, event)">
                         </i>
                                <img src="/uploads/<%= product.image[0] %>" alt="Product Image" class="card-img-top">
                            </div>
                            <div class="card-body">
                                <h5 class="card-title">
                                    <%= product.brand %>
                                </h5>
                                <p class="card-text-model">Model: <%= product.name %>
                                </p>
                                <p class="card-text-price">₹<%= product.price.toFixed(2) %>
                                </p>
                            </div>
                        </div>
                    </a>
                    <% }); %>
                    <% } %>
            </div>
        </div>
        

        <!-- Pagination -->
        <div class="container text-center my-4" style="padding-left: 650px;">
            <div class="pagination">
                <% if (currentPage > 1) { %>
                    <a href="/shopwatches?page=<%= currentPage - 1 %>" class="btn btn-secondary">Previous</a>
                <% } %>


                    <% for (let i = 1; i <= totalPages; i++) { %>
                        <a href="/shopwatches?page=<%= i %>" class="btn <%= currentPage === i ? 'btn-primary' : 'btn-light' %>"><%= i %></a>
                    <% } %>


                    <% if (currentPage < totalPages) { %>
                        <a href="/shopwatches?page=<%= currentPage + 1 %>" class="btn btn-secondary">Next</a>
                    <% } %>
            </div>
        </div>
    </div>





    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script>

        document.getElementById('priceRange').addEventListener('input', function () {
            const priceDisplay = document.getElementById('priceDisplay');
            priceDisplay.textContent = `Selected Price: ₹${this.value}`;
        });

        function clearFilters() {
        window.location.href = '/shopWatches';
    }
    function toggleWishlist(icon, event) {
    event.preventDefault(); 

    const productId = icon.getAttribute('data-product-id');
    const isLiked = icon.classList.contains('liked');
    const url = isLiked ? `/wishlist/remove/${productId}` : `/wishlist/add/${productId}`;
    const method = 'POST';

    fetch(url, { method })
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                icon.classList.toggle('liked');
               
                if (icon.classList.contains('liked')) {
                    icon.classList.add('fas');
                    icon.classList.remove('far');
                } else {
                    icon.classList.add('far');
                    icon.classList.remove('fas');
                }
            } else {
                console.error('Failed to update wishlist');
            }
        })
        .catch(error => console.error('Error:', error));
}
    </script>
</body>

</html>